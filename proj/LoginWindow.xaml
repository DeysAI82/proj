<Window x:Class="proj.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:proj.ViewModels"
        mc:Ignorable="d"
        Title="Login" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Email:" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Margin="100,0,0,5" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Width="200"/>

        <TextBlock Text="Password:" Grid.Row="1" VerticalAlignment="Center"/>
        <PasswordBox Grid.Row="1" Margin="100,0,0,5" Width="200"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Login" Width="100" Margin="5" Command="{Binding LoginCommand}"/>
            <Button Content="Register" Width="100" Margin="5" Command="{Binding OpenRegisterCommand}"/>
        </StackPanel>
    </Grid>
</Window>
