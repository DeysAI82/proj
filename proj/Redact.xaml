<Window x:Class="proj.Redact"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:proj.ViewModels"
        mc:Ignorable="d"
        Title="Выбор техники" Height="450" Width="600">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListView ItemsSource="{Binding Equipments}"
                  SelectedItem="{Binding SelectedEquipment}"
                  Grid.Row="0"
                  Margin="0 0 0 10"
                  BorderThickness="1"
                  BorderBrush="Gray">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="1" BorderBrush="#ccc" Width="400" Padding="5" Margin="3">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="{Binding Model, StringFormat=Модель: {0}}" />
                            <TextBlock Text="{Binding DailyRate, StringFormat=Цена/день: {0}₽}" />
                            <TextBlock Text="{Binding Status, StringFormat=Статус: {0}}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Content="Арендовать"
                Grid.Row="1"
                Height="40"
                Command="{Binding RentCommand}"
                />
        <Button Content="Меню"
                Grid.Row="2"
                Height="40" Click="Button_Click"/>
    </Grid>

</Window>
